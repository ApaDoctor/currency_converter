sudo: required
dist: trusty

services:
- docker

env:
  - TRAVIS_CI=True

install:
- docker-compose pull
- docker-compose build

script:
- docker-compose up --build -d
- docker-compose exec app /venv/bin/python -m pytest
